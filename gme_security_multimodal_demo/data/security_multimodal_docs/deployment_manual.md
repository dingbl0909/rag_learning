# 部署排障与系统运维

## Milvus 检索异常排查

如果部署后的知识系统无法正确检索，应依次检查集合是否加载、向量维度是否一致、索引字段是否匹配，以及解析服务、嵌入服务和检索服务之间的依赖状态。

TABLE_REF: milvus_checklist_table

## OCR 与嵌入服务联调

在私有化部署中，需要确保 Dots.OCR、嵌入模型服务和问答模型服务之间的调用协议一致，并确认图片 Base64、表格标签和段落信息能够稳定传递到后续索引链路。
