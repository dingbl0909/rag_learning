# 设备接入与项目部署

## 摄像头接入排查

摄像头接入平台后如果持续离线，应依次核对设备编码、IP 地址、供电状态、网络连通性、时间同步和鉴权配置。部署人员需要保留设备清单和端口映射关系，避免在批量接入时出现重复注册或组织树挂载错误。

## 项目部署故障定位

若安防知识系统部署在容器环境后无法检索，应首先确认 Milvus 集合是否已加载，再检查向量维度、索引字段和写入任务是否一致。同时需要检查 OCR 服务、嵌入服务和问答服务之间的依赖关系是否健康。

TABLE_REF: deployment_checklist_table
